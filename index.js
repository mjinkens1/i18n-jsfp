const {
  addTranslations,
  configure,
  setCurrentLocale,
  setFallbackLocale,
  translate,
} = require('./dist');

module.exports = {
  addTranslations,
  configure,
  setCurrentLocale,
  setFallbackLocale,
  translate,
};
