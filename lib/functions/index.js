import addTranslations from './addTranslations';
import addTranslationsMulti from './addTranslationsMulti';
import configure from './configure';
import setCurrentLocale from './setCurrentLocale';
import setFallbackLocale from './setFallbackLocale';
import translate from './translate';

const functions = {
  addTranslations,
  addTranslationsMulti,
  configure,
  setCurrentLocale,
  setFallbackLocale,
  translate,
};

export {
  addTranslations,
  addTranslationsMulti,
  configure,
  setCurrentLocale,
  setFallbackLocale,
  translate,
};

export default functions;
